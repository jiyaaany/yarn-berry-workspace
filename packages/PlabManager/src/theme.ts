import { Theme } from '@emotion/native';
import { SemanticColors, Colors } from './types/theme';

const semantic: SemanticColors = {
  primary: '#000',
};

const colors: Colors = {
  semantic,
};

export const theme: Theme = {
  colors,
};
